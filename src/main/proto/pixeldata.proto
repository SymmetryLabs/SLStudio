syntax = "proto3";

package com.symmetrylabs.slstudio.streaming;

option java_multiple_files = true;

message PixelData {
    bytes pixelData = 1;
}

message PixelDataRequest {
}

service PixelDataService {
    rpc Subscribe(PixelDataRequest) returns (stream PixelData) {}
}
