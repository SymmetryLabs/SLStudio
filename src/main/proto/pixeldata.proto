syntax = "proto3";

package com.symmetrylabs.slstudio.streaming;

option java_multiple_files = true;

message Pixels {
    bytes colors = 1;
}

message PixelDataRequest {
}

service PixelDataService {
    rpc Subscribe(PixelDataRequest) returns (stream Pixels) {}
}
