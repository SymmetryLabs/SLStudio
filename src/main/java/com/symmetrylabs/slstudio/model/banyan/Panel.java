package com.symmetrylabs.slstudio.model.banyan;

import com.symmetrylabs.slstudio.model.SLModel;
import heronarts.lx.model.EmptyFixture;
import heronarts.lx.model.LXFixture;

public abstract class Panel extends SLModel {
    public Panel(String modelId, LXFixture fixture) {
        super(modelId, fixture); // temp, just to get it to build
    }
}
