package com.symmetrylabs.slstudio.model;

import java.util.ArrayList;

/*
* Class describes a contiguous run of strip (connected in serial)
* which is meant to be driven by a single data in.
* Can be added to an output as follows lx.addOutput(stripRun)
 */
public class StripRun {
    private ArrayList<Strip> strips;

    public StripRun(ArrayList<Strip> strips){

    }
}
